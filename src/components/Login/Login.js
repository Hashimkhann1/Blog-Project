import React from 'react'

const Login = ({subHandle}) => {


    return (
        <div>
            <p>Login</p>
            <button onClick={subHandle}>Login</button>
        </div>
    )
}

export default Login
